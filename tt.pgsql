create table test (
    id SERIAL primary key,
    name varchar(10)
);

create table test2 (
    id serial primary key,
    score integer,
    array(name) .
);

insert into test values (default , 'ali' );
insert into test values (default , 'hg');
select * from test;
